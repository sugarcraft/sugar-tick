name: CI

on:
  push:
    branches: [master]
  pull_request:

env:
  # Belt-and-suspenders: every action below is already pinned to its
  # Node-24-native major (checkout@v5, cache@v5, setup-php@v2 ≥2.37),
  # but this flag keeps any transitively-loaded JavaScript action that
  # still defaults to Node 20 from generating deprecation warnings.
  # Requires Actions Runner ≥ 2.327.1 (already the default on
  # ubuntu-latest as of 2025-Q4).
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

  # Single source of truth for the PHP extension union — referenced by
  # both the cache-extensions step (for cache-key computation) and the
  # setup-php step (for the actual install). See the test job for why
  # we install everything on every cell.
  PHP_EXTENSIONS: mbstring, intl, json, pcntl, sqlite3, pdo_sqlite, ssh2, gd, curl, openssl, ffi

jobs:
  # Unified test matrix — single job, four PHP versions, with per-lib include
  # overrides to skip libs that don't support certain PHP majors.
  #
    # Lib exclusions by PHP version:
    #   PHP 8.1  — excluded: candy-vt, candy-vcr (^8.2), candy-wish (^8.3), sugar-crush (transitive ^8.3 dep)
    #   PHP 8.2  — excluded: candy-wish (^8.3)
    #   PHP 8.3+ — all libs
  test:
    name: 'Test PHP ${{ matrix.php }} · ${{ matrix.lib }}'
    runs-on: ubuntu-latest
    concurrency:
      group: ci-test-${{ github.event_name }}-${{ github.ref }}-${{ matrix.lib }}-${{ matrix.php }}
      cancel-in-progress: ${{ github.event_name == 'push' }}
    strategy:
      fail-fast: false
      matrix:
        php: ['8.1', '8.2', '8.3', '8.4']
        lib:
          - candy-core
          - candy-sprinkles
          - candy-pty
          - honey-bounce
          - candy-zone
          - sugar-bits
          - sugar-charts
          - sugar-prompt
          - candy-shell
          - candy-shine
          - candy-kit
          - candy-freeze
          - sugar-glow
          - sugar-spark
          - sugar-wishlist
          - candy-mold
          - candy-tetris
          - super-candy
          - sugar-stash
          - candy-query
          - sugar-tick
          - candy-mosaic
          - honey-flap
          - candy-hermit
          - candy-lister
          - candy-log
          - candy-palette
          - candy-serve
          - sugar-boxer
          - sugar-calendar
          - sugar-crumbs
          - sugar-post
          - sugar-readline
          - sugar-skate
          - sugar-stickers
          - sugar-table
          - sugar-toast
          - sugar-veil
        include:
          # candy-vt / candy-vcr — PHP ^8.2, not 8.1
          - php: '8.2'
            lib: candy-vt
          - php: '8.3'
            lib: candy-vt
          - php: '8.4'
            lib: candy-vt
          - php: '8.2'
            lib: candy-vcr
          - php: '8.3'
            lib: candy-vcr
          - php: '8.4'
            lib: candy-vcr
          # candy-wish — PHP ^8.3, not 8.1 or 8.2
          - php: '8.3'
            lib: candy-wish
          - php: '8.4'
            lib: candy-wish
          # sugar-crush — transitive ^8.3 dep (bubbletea), excluded from 8.1/8.2
          - php: '8.2'
            lib: sugar-crush
          - php: '8.3'
            lib: sugar-crush
          - php: '8.4'
            lib: sugar-crush
          # candy-metrics — composer.lock was stale; runs on all 8.3+ cells
          - php: '8.3'
            lib: candy-metrics
          - php: '8.4'
            lib: candy-metrics

    steps:
      - uses: actions/checkout@v5

      # Cache the *built* PHP extension binaries (ssh2 / gd / curl / sqlite3
      # / pdo_sqlite — the ones that aren't bundled with the runner image).
      # Without this, every matrix cell rebuilds them from PECL, adding
      # 5–10s × 164 cells = ~20 minutes of total CPU per push.
      - name: Compute PHP-extension cache key
        id: ext-cache
        uses: shivammathur/cache-extensions@v1
        with:
          php-version: ${{ matrix.php }}
          extensions: ${{ env.PHP_EXTENSIONS }}
          # Bump key suffix when adding/removing extensions or when a runner
          # image upgrade invalidates ABI compatibility.
          key: php-ext-v3

      - name: Restore PHP extensions
        uses: actions/cache@v5
        with:
          path: ${{ steps.ext-cache.outputs.dir }}
          key: ${{ steps.ext-cache.outputs.key }}
          restore-keys: ${{ steps.ext-cache.outputs.key }}

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: ${{ env.PHP_EXTENSIONS }}
          tools: composer:v2
          coverage: none

      - name: Get composer cache dir
        id: cache
        run: |
          echo "dir=$(composer config cache-files-dir)" >> "$GITHUB_OUTPUT"

      - name: Cache composer downloads
        uses: actions/cache@v5
        with:
          path: ${{ steps.cache.outputs.dir }}
          key: composer-${{ matrix.php }}-${{ hashFiles('**/composer.json') }}
          restore-keys: |
            composer-${{ matrix.php }}-

      # Cache the per-lib vendor/ directory keyed on its composer.json. When
      # only one lib changes, every other lib's vendor cache hits and
      # composer install becomes a ~100ms validation pass. The fallback
      # restore-keys lets a sibling-lib's cache seed dependency resolution
      # before composer fills in the rest from the warm composer cache.
      - name: Cache vendor/
        uses: actions/cache@v5
        with:
          path: ${{ matrix.lib }}/vendor
          key: vendor-${{ matrix.php }}-${{ matrix.lib }}-${{ hashFiles(format('{0}/composer.json', matrix.lib)) }}
          restore-keys: |
            vendor-${{ matrix.php }}-${{ matrix.lib }}-

      - name: Install dependencies
        working-directory: ${{ matrix.lib }}
        run: composer install --prefer-dist --no-progress

      - name: Run tests
        working-directory: ${{ matrix.lib }}
        run: vendor/bin/phpunit --colors=never

  # Windows test job — verifies Windows-native TTY paths (FFI/kernel32 paths
  # in candy-core) pass on windows-latest. Uses a small matrix of the most
  # TTY-sensitive libs to keep wall-clock time reasonable.
  windows-test:
    name: 'Windows · PHP ${{ matrix.php }} · ${{ matrix.lib }}'
    runs-on: windows-2025
    # Cancel an in-progress run of the same cell when a fresh push lands.
    concurrency:
      group: ci-windows-${{ github.event_name }}-${{ github.ref }}-${{ matrix.lib }}-${{ matrix.php }}
      cancel-in-progress: ${{ github.event_name == 'push' }}
    strategy:
      fail-fast: false
      matrix:
        php: ['8.2', '8.3']
        lib:
          - candy-core
          - sugar-prompt
          - sugar-bits
          - candy-shell
          - candy-shine
    steps:
      - uses: actions/checkout@v5

      # PHP on Windows comes with FFI enabled by default in PHP 8.2+.
      # We still declare mbstring / intl / json / pcntl explicitly so that
      # the action installs them if they are missing from the runner image.
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, intl, json, pcntl, shmop, ffi
          tools: composer:v2
          coverage: none
          ini-values: ffi.enable=true

      # Composer cache disabled on Windows: path encoding issues in GH Actions
      # runner make the cache more trouble than it's worth.  Vendor/ caching
      # (below) handles the significant per-lib dependency layer.

      - name: Cache vendor/
        uses: actions/cache@v5
        with:
          path: ${{ matrix.lib }}/vendor
          key: vendor-win-${{ matrix.php }}-${{ matrix.lib }}-${{ hashFiles(format('{0}/composer.json', matrix.lib)) }}
          restore-keys: |
            vendor-win-${{ matrix.php }}-${{ matrix.lib }}-

      - name: Install dependencies
        working-directory: ${{ matrix.lib }}
        run: composer install --prefer-dist --no-progress

      # candy-core: run TTY subsystem tests (where WindowsBackend / kernel32 FFI
      # lives).  Downstream libs (sugar-prompt, sugar-bits, candy-shell,
      # candy-shine): run full suite — any failures there are pre-existing and
      # unrelated to the x/windows changes.
      - name: Run tests
        working-directory: ${{ matrix.lib }}
        run: |
          if (Test-Path tests\Util\Tty) {
            vendor\bin\phpunit tests\Util\Tty\ --colors=never
          } else {
            vendor\bin\phpunit --colors=never
          }

  # macOS test job — verifies POSIX PTY paths (candy-pty's libc FFI:
  # posix_openpt / grantpt / unlockpt / ptsname_r / ioctl + the macOS
  # TIOCSWINSZ / TIOCGWINSZ constants that diverge from Linux) work
  # on Darwin. Scoped to candy-pty only — other libs would catch
  # macOS-specific regressions on demand if/when issues surface.
  macos-test:
    name: 'macOS · PHP ${{ matrix.php }} · ${{ matrix.lib }}'
    runs-on: macos-latest
    concurrency:
      group: ci-macos-${{ github.event_name }}-${{ github.ref }}-${{ matrix.lib }}-${{ matrix.php }}
      cancel-in-progress: ${{ github.event_name == 'push' }}
    strategy:
      fail-fast: false
      matrix:
        php: ['8.2', '8.3']
        lib:
          - candy-pty
    steps:
      - uses: actions/checkout@v5

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: mbstring, intl, json, pcntl, ffi
          tools: composer:v2
          coverage: none
          ini-values: ffi.enable=true

      - name: Cache vendor/
        uses: actions/cache@v5
        with:
          path: ${{ matrix.lib }}/vendor
          key: vendor-mac-${{ matrix.php }}-${{ matrix.lib }}-${{ hashFiles(format('{0}/composer.json', matrix.lib)) }}
          restore-keys: |
            vendor-mac-${{ matrix.php }}-${{ matrix.lib }}-

      - name: Install dependencies
        working-directory: ${{ matrix.lib }}
        run: composer install --prefer-dist --no-progress

      - name: Run tests
        working-directory: ${{ matrix.lib }}
        run: vendor/bin/phpunit --colors=never

  # Coverage report — runs once on PHP 8.3 + pcov for every lib in the
  # matrix, uploads a per-lib clover XML to codecov with the lib slug
  # as a flag. Codecov uses the flag to surface a separate badge per
  # package on the README.
  coverage:
    name: 'Coverage · ${{ matrix.lib }}'
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    # Same logic as the test job: when a newer push to master makes a previous
    # coverage run stale, cancel the old one so we don't waste minutes on a
    # report that's about to be replaced. The job's `if:` already restricts
    # this to push-on-master, so the group key only needs lib granularity.
    concurrency:
      group: ci-coverage-${{ github.ref }}-${{ matrix.lib }}
      cancel-in-progress: true
    strategy:
      fail-fast: false
      matrix:
        lib:
          - candy-core
          - candy-sprinkles
          - candy-pty
          - honey-bounce
          - candy-zone
          - sugar-bits
          - sugar-charts
          - sugar-prompt
          - candy-shell
          - candy-shine
          - candy-kit
          - candy-freeze
          - sugar-glow
          - sugar-spark
          - sugar-wishlist
          - candy-mold
          - candy-mosaic
          - candy-tetris
          - super-candy
          - sugar-stash
          - candy-query
          - sugar-tick
          - candy-mines
          - candy-flip
          - honey-flap
          - candy-hermit
          - candy-lister
          - candy-log
          - candy-palette
          - candy-serve
          - sugar-boxer
          - sugar-calendar
          - sugar-crumbs
          - sugar-post
          - sugar-readline
          - sugar-skate
          - sugar-stickers
          - sugar-table
          - sugar-toast
          - sugar-veil
    steps:
      - uses: actions/checkout@v5

      # Same extension-cache pattern as the test job (see comment there).
      - name: Compute PHP-extension cache key
        id: ext-cache
        uses: shivammathur/cache-extensions@v1
        with:
          php-version: '8.3'
          extensions: ${{ env.PHP_EXTENSIONS }}
          key: php-ext-cov-v3

      - name: Restore PHP extensions
        uses: actions/cache@v5
        with:
          path: ${{ steps.ext-cache.outputs.dir }}
          key: ${{ steps.ext-cache.outputs.key }}
          restore-keys: ${{ steps.ext-cache.outputs.key }}

      - name: Setup PHP (with pcov)
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'
          # Same extension union as the test job — keeps test- and
          # coverage-job environments byte-identical.
          extensions: ${{ env.PHP_EXTENSIONS }}
          tools: composer:v2
          coverage: pcov

      - name: Cache composer downloads
        uses: actions/cache@v5
        with:
          path: ~/.composer/cache/files
          key: composer-cov-${{ hashFiles('**/composer.json') }}
          restore-keys: |
            composer-cov-

      - name: Cache vendor/
        uses: actions/cache@v5
        with:
          path: ${{ matrix.lib }}/vendor
          key: vendor-cov-${{ matrix.lib }}-${{ hashFiles(format('{0}/composer.json', matrix.lib)) }}
          restore-keys: |
            vendor-cov-${{ matrix.lib }}-

      - name: Install dependencies
        working-directory: ${{ matrix.lib }}
        run: composer install --prefer-dist --no-progress

      - name: Run tests with coverage
        working-directory: ${{ matrix.lib }}
        run: vendor/bin/phpunit --colors=never --coverage-clover=coverage.xml

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          # Explicit slug + a single named file → don't accidentally
          # walk the whole tree looking for other coverage reports.
          slug: detain/sugarcraft
          files: ${{ matrix.lib }}/coverage.xml
          disable_search: true
          flags: ${{ matrix.lib }}
          name: ${{ matrix.lib }}
          # Token MUST be set as a secret on detain/sugarcraft (repo or
          # environment scope) — GitHub org secrets only flow into repos
          # *inside* that org, so a sugarcraft-org-level secret won't be
          # visible to this monorepo. For a public repo the action falls
          # back to tokenless OIDC upload when the secret is empty, so
          # this still works during initial bootstrap.
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false
          verbose: true

  # PHPStan static analysis — runs on all PHP versions after tests pass,
  # verifying type safety and catching type errors before they reach CI.
  # Only runs on libs that have a phpstan.neon file.
  phpstan:
    name: 'PHPStan ${{ matrix.php }} · ${{ matrix.lib }}'
    runs-on: ubuntu-latest
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/master' || github.event_name == 'pull_request'
    concurrency:
      group: ci-phpstan-${{ github.event_name }}-${{ github.ref }}-${{ matrix.lib }}-${{ matrix.php }}
      cancel-in-progress: ${{ github.event_name == 'push' }}
    strategy:
      fail-fast: false
      matrix:
        php: ['8.1', '8.2', '8.3', '8.4']
        lib:
          - candy-core  # has phpstan.neon
    steps:
      - uses: actions/checkout@v5

      - name: Compute PHP-extension cache key
        id: ext-cache
        uses: shivammathur/cache-extensions@v1
        with:
          php-version: ${{ matrix.php }}
          extensions: ${{ env.PHP_EXTENSIONS }}
          key: php-ext-stan-v3

      - name: Restore PHP extensions
        uses: actions/cache@v5
        with:
          path: ${{ steps.ext-cache.outputs.dir }}
          key: ${{ steps.ext-cache.outputs.key }}
          restore-keys: ${{ steps.ext-cache.outputs.key }}

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          extensions: ${{ env.PHP_EXTENSIONS }}
          tools: composer:v2
          coverage: none

      - name: Cache composer downloads
        uses: actions/cache@v5
        with:
          path: ~/.composer/cache/files
          key: composer-stan-${{ matrix.php }}-${{ hashFiles('**/composer.json') }}
          restore-keys: |
            composer-stan-${{ matrix.php }}-
            composer-stan-

      - name: Cache vendor/
        uses: actions/cache@v5
        with:
          path: ${{ matrix.lib }}/vendor
          key: vendor-stan-${{ matrix.php }}-${{ matrix.lib }}-${{ hashFiles(format('{0}/composer.json', matrix.lib)) }}
          restore-keys: |
            vendor-stan-${{ matrix.php }}-${{ matrix.lib }}-
            vendor-stan-${{ matrix.lib }}-

      - name: Install dependencies
        working-directory: ${{ matrix.lib }}
        run: composer install --prefer-dist --no-progress

      - name: Run PHPStan
        working-directory: ${{ matrix.lib }}
        run: vendor/bin/phpstan analyse --level=5 --no-progress
