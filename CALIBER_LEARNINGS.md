# Caliber Learnings

Accumulated patterns and anti-patterns from development sessions.
Auto-managed by [caliber](https://github.com/caliber-ai-org/ai-setup) — do not edit manually.

- **[gotcha]** When threading a new `&$flag` reference variable through nested closures in PHP, you must add it to EVERY `use()` clause along the path — adding `&$firstResizeSkipped` to the inner `dispatchEvent` closure but forgetting the outer `$step = function () use (...)` closure produces `TypeError: Argument #N (...) must be of type bool, null given` at runtime, NOT a compile-time error. The variable resolves to `null` in the outer scope and gets passed down. Audit every nested `use(...)` when adding a closure-captured variable; `grep -n "use (" Player.php` lists them all in one pass.
- **[pattern]** When a `ByteAssertion`-style diff helper produces a hex window starting at the first-divergence offset (not at offset 0), tests asserting on the diff string cannot expect the FULL hex of either expected/actual — the window only contains the divergent tail. Force the divergence to offset 0 (e.g. `str_repeat('A', 16)` vs `str_repeat('B', 16)`) when you want to assert the full 16-byte hex window.
- **[convention]** `ScreenAssertion` requires `sugarcraft/candy-vt` — add it under `require-dev` (NOT `require`) of consuming libs since cell-grid replay assertion is a test-time concern, with `"sugarcraft/candy-vt": "@dev"` plus a `repositories` path entry. The class throws `\RuntimeException` if `Terminal::class` doesn't exist, which is the explicit fallback signal to use `ByteAssertion` instead.
- **[convention]** `ScreenAssertion` requires `sugarcraft/candy-vt` — add it under `require-dev` (NOT `require`) of consuming libs since cell-grid replay assertion is a test-time concern, with `"sugarcraft/candy-vt": "@dev"` plus a `repositories` path entry. The class throws `\RuntimeException` if `Terminal::class` doesn't exist, which is the explicit fallback signal to use `ByteAssertion` instead. Same shape for `YamlFormat` — `symfony/yaml` lives under `require-dev` (^6.4 || ^7.0), with a `suggest` entry pointing users to it; the class throws `\RuntimeException` at construction if `Yaml::class` is missing.
- **[gotcha]** The Bash tool's working directory persists across separate Bash calls. After `cd /home/sites/sugarcraft/<lib> && cmd`, the NEXT Bash call still starts in `<lib>` — `git status` will then show paths with `../` prefixes that look like they're outside the repo but aren't. A bare `ls candy-core/src/` from inside `candy-core` returns NO output (empty `{}` response from the tool, not a `No such file or directory` error) because the path is relative to the wrong cwd. Bare `grep candy-core/src/Cmd.php` after `cd .../candy-vcr` produces `ugrep: warning: candy-core/src/Cmd.php: No such file or directory`. Worse, a compound command like `git status --short && git add foo bar baz && git status --short` can return an EMPTY `{}` response (no stdout, no stderr, no exit-code error) when the cwd has drifted to a worktree state where the chained command finds nothing. Always anchor with `cd /home/sites/sugarcraft && ...` to start from the repo root, or pass absolute paths to every command. Same trap bites HEREDOC-written `/tmp/dbg.php` scripts: `require_once __DIR__ . '/vendor/autoload.php'` resolves `__DIR__` to `/tmp/`, NOT to the working dir of the heredoc-emitting Bash call. Use absolute autoload paths (`require_once '/home/sites/sugarcraft/<lib>/vendor/autoload.php';`) in any standalone PHP script you stage under `/tmp/`.
- **[gotcha]** PSR-4 autoload requires one class per file with basename = class name. Putting test helper/fixture classes (e.g. `UserJsonableMsg`, `TickModel`) at the bottom of a `FooTest.php` works ONLY because PHPUnit `require`s `FooTest.php` to instantiate the test, side-effect-loading the inline classes. If a SECOND test file references those fixtures and runs first in isolation (e.g. `phpunit --filter BarTest`), autoload fails — there is no `tests/.../UserJsonableMsg.php` for the autoloader to find. Defining a `final class TickModel` at the bottom of `tests/Assert/ScreenRoundTripTest.php` collides at autoload time if `tests/PlayerTest.php` also defines its own `TickModel` — both files declare the same FQCN even though each is local to one test, causing `Cannot declare class … because the name is already in use` when both load in the same suite. Either use a different per-file class name or extract the fixture to a single shared file.
- **[gotcha]** A `candy-vcr` round-trip test (record session A → replay into fresh Program B → assert byte-equal) is NOT achievable with `ByteAssertion` alone — recording captures whatever the framerate-based renderer emitted during the recording loop's lifespan, the replay loop runs over a different chained-timer timeline (Player yields between events to let render fire) and emits a *cell-grid equivalent* but byte-different stream. For round-trip parity use `ScreenAssertion` (cell-grid via candy-vt) — feeds both byte streams into separate `Terminal` instances and compares grapheme grids via `Screen::diff()`, which tolerates ANSI-level reordering (redundant SGR re-emission, equivalent cursor-position sequences, partial vs full repaints).
- **[pattern]** For YAML cassettes / human-readable serialization formats backing a value-object (e.g. `Cassette` with header + event list), use `Symfony\Component\Yaml\Yaml::dump($tree, inline: 3, indent: 4)` — `inline: 3` flows the per-event maps onto one line each (compact, diff-friendly) while keeping `header:` and `events:` at the top level on their own lines. Both YAML and JSONL formats should round timestamps the same way (e.g. `round($t, 3)` for ms precision) so cross-format equality holds via the shared value object — tests should explicitly assert this with a `testCrossFormatJsonlYamlRoundTrip` that decodes the same `Cassette` through both formats and compares per-event `t`/`kind`/`payload` triples.
- **[gotcha]** A `candy-vcr` `Player` driving a `Program` in `SPEED_INSTANT` mode must NOT schedule every cassette event at `delay=0` via `addTimer` — React's `StreamSelectLoop` fires them all in a single tick before the program's framerate-based render tick gets a turn, so `view()` is never rendered and no output bytes are produced (`outputCount: 0` despite `outputCount > 0` in the cassette). Schedule events sequentially with `Player::INSTANT_YIELD_SECONDS` (~5 ms) between events; in `SPEED_REALTIME` use recorded `t` deltas. Replay programs need a high `framerate` (e.g. 1000.0 in `ProgramOptions`) so the renderer wakes up on the yield interval rather than the default ~60 Hz.
- **[gotcha]** A test helper method named `run()` on a `PHPUnit\Framework\TestCase` subclass dies at autoload with `PHP Fatal error: Cannot override final method PHPUnit\Framework\TestCase::run()` — `TestCase::run()` is final in PHPUnit 10. Pick any other name (`exec()`, `invoke()`, `dispatch()`) for helpers that drive the system-under-test; the same trap also applies to other final TestCase methods like `runBare()` and `runTest()`. Symptom looks like a compile error in the test file itself, not a PHPUnit upgrade issue.
- **[pattern]** PHP `bin/` shim scripts that need to work both from the lib's own `vendor/` (development) AND when composer-installed under a parent project's `vendor/bin/` (the bin shim copy) should iterate two candidate autoload paths: `__DIR__ . '/../vendor/autoload.php'` then `__DIR__ . '/../../../autoload.php'`. The first wins in the lib's own checkout; the second wins when composer installs the bin under `vendor/<vendor>/<pkg>/bin/<script>` and `vendor/autoload.php` is three levels up. Then `composer.json` declares `"bin": ["bin/<script>"]` so composer auto-symlinks it into the consumer's `vendor/bin/`.
- **[gotcha]** A PHP `bin/` shim must be `chmod +x` in the working tree before staging — `git add` preserves the mode bit, but creating the file via the Write tool does NOT mark it executable. Run `chmod +x candy-vcr/bin/<script>` immediately after Write so the committed mode is `100755`; otherwise composer's bin-symlink will install a non-executable file and `vendor/bin/<script>` will fail with `Permission denied`.
- **[gotcha]** The Bash tool's working directory persists across separate Bash calls. After `cd /home/sites/sugarcraft/<lib> && cmd`, the NEXT Bash call still starts in `<lib>` — `git status` will then show paths with `../` prefixes that look like they're outside the repo but aren't. A bare `ls candy-core/src/` from inside `candy-core` returns NO output (empty `{}` response from the tool, not a `No such file or directory` error) because the path is relative to the wrong cwd. Bare `grep candy-core/src/Cmd.php` after `cd .../candy-vcr` produces `ugrep: warning: candy-core/src/Cmd.php: No such file or directory` (and same for `grep candy-vcr/src/Player.php` after `cd /home/sites/sugarcraft/candy-vcr` — the file is at `src/Player.php` from inside that cwd). Worse, a compound command like `git status --short && git add foo bar baz && git status --short` can return an EMPTY `{}` response (no stdout, no stderr, no exit-code error) when the cwd has drifted to a worktree state where the chained command finds nothing. Always anchor with `cd /home/sites/sugarcraft && ...` to start from the repo root, or pass absolute paths to every command. Same trap bites HEREDOC-written `/tmp/dbg.php` scripts: `require_once __DIR__ . '/vendor/autoload.php'` resolves `__DIR__` to `/tmp/`, NOT to the working dir of the heredoc-emitting Bash call. Use absolute autoload paths (`require_once '/home/sites/sugarcraft/<lib>/vendor/autoload.php';`) in any standalone PHP script you stage under `/tmp/`.
- **[gotcha]** A Bash chain like `ls foo 2>/dev/null && echo "---" && ls bar` returns an EMPTY `{}` response from the Bash tool when the first command exits non-zero (e.g. `ls candy-pty` when the dir doesn't exist) — `&&` short-circuits, the entire rest of the chain is suppressed, and the tool surface gives back nothing rather than an error. Recovery: wrap the maybe-failing probe in `( ... || echo "<sentinel>" )` so the chain continues regardless: `(ls -la candy-pty 2>/dev/null || echo "candy-pty does not exist") && echo "---" && ls plans/`. Or use `;` instead of `&&` for unrelated checks. Same root cause as the cwd-drift empty-`{}` trap — any non-zero exit early in a `&&` chain produces a silent black-hole response.
- **[convention]** Plans under `plans/*.md` may carry an explicit conditional gate (e.g. `plans/x-xpty.md`: "only worth doing if we're going to upgrade candy-wish to host SSH sessions in-process … Document the upgrade path; don't ship until the use case is green-lit"). Before starting a slice, scan for phrases like "Conditional plan", "don't ship until", "only worth doing if", "green-lit" and confirm the gate is satisfied — `git log` + a `grep -r` for the conditional consumer + the issue tracker. If the gate isn't satisfied, surface that to the user before scaffolding instead of charging ahead.
- **[pattern]** When a `ByteAssertion`-style diff helper produces a hex window starting at the first-divergence offset (not at offset 0), tests asserting on the diff string cannot expect the FULL hex of either expected/actual — the window only contains the divergent tail. Force the divergence to offset 0 (e.g. `str_repeat('A', 16)` vs `str_repeat('B', 16)`) when you want to assert the full 16-byte hex window (`4141…41` / `4242…42`).
- **[gotcha]** The Bash tool's working directory persists across separate Bash calls. After `cd /home/sites/sugarcraft/<lib> && cmd`, the NEXT Bash call still starts in `<lib>` — `git status` will then show paths with `../` prefixes that look like they're outside the repo but aren't. A bare `ls candy-core/src/` from inside `candy-core` returns NO output (empty `{}` response from the tool, not a `No such file or directory` error) because the path is relative to the wrong cwd. Bare `grep candy-core/src/Cmd.php` after `cd .../candy-vcr` produces `ugrep: warning: candy-core/src/Cmd.php: No such file or directory` (e.g. `grep .github/workflows/vhs.yml` from a lib subdir gives `ugrep: warning: .github/workflows/vhs.yml: No such file or directory` even though the file exists at the repo root). Worse, a compound command like `git status --short && git add foo bar baz && git status --short` can return an EMPTY `{}` response (no stdout, no stderr, no exit-code error) when the cwd has drifted to a worktree state where the chained command finds nothing. Always anchor with `cd /home/sites/sugarcraft && ...` to start from the repo root, or pass absolute paths to every command. Same trap bites HEREDOC-written `/tmp/dbg.php` scripts: `require_once __DIR__ . '/vendor/autoload.php'` resolves `__DIR__` to `/tmp/`, NOT to the working dir of the heredoc-emitting Bash call. Use absolute autoload paths (`require_once '/home/sites/sugarcraft/<lib>/vendor/autoload.php';`) in any standalone PHP script you stage under `/tmp/`.
- **[gotcha]** PSR-4 autoload requires one class per file with basename = class name. Putting test helper/fixture classes (e.g. `UserJsonableMsg`, `TickModel`) at the bottom of a `FooTest.php` works ONLY because PHPUnit `require`s `FooTest.php` to instantiate the test, side-effect-loading the inline classes. If a SECOND test file references those fixtures and runs first in isolation (e.g. `phpunit --filter BarTest`), autoload fails. Same trap: defining a `final class TickModel` at the bottom of `tests/Assert/ScreenRoundTripTest.php` collides at autoload time if `tests/PlayerTest.php` also defines its own `TickModel` — both files declare the same FQCN even though each is local to one test, causing `Cannot declare class … because the name is already in use` when both load in the same suite. Either use a different per-file class name or extract the fixture to a single shared file.
- **[gotcha]** `.github/workflows/ci.yml` contains TWO separate matrix `lib:` lists (one for the test job ~line 45, one for a downstream job ~line 239) — adding a new lib to only one means it is silently skipped by the other job. Use `Edit ... replace_all: true` on the matrix-row pattern, or `grep -n "<old-lib>" ci.yml` first to enumerate every occurrence. Verify after with `grep -n "<new-lib>" .github/workflows/ci.yml` — expect ≥2 lines.
- **[gotcha]** `.github/workflows/vhs.yml` does NOT use a YAML `matrix:` list for libs — it uses a Bash array `all=( candy-core candy-sprinkles … )` inside an `id: compute` step (around lines 51-69). AGENTS.md says "matrix lib: entry" but that's only accurate for `ci.yml`. To register a new VHS-eligible lib, edit the bash array tokens (whitespace-separated, multi-line) — `grep` for `candy-vt` to locate the right slot. Libs with no `.vhs/` demo don't need to be added until they have a tape.
- **[pattern]** For FFI-dependent test classes (e.g. `candy-pty`'s `Pty::open()` round-trip), do NOT put the FFI/`/dev/ptmx`/PHP_OS_FAMILY skip checks in `setUp()` — that skips the entire class on FFI-less CI runners and you lose coverage on structural tests (cdef shape, env-override resolution, exception hierarchy) that don't actually touch the syscalls. Instead extract a private `requirePtySyscalls()` (or similarly named) helper and call it as the FIRST line of each test that opens a real pty. Structural tests run unconditionally; round-trip tests skip cleanly when the host can't support them.
- **[gotcha]** `.github/workflows/ci.yml` carries TWO separate ext-cache key suffixes — `key: php-ext-v<N>` (~line 100, test job) and `key: php-ext-cov-v<N>` (~line 290, coverage job). When you change the `PHP_EXTENSIONS` env (e.g. add `ffi` for candy-pty), you MUST bump BOTH suffixes (`v2` → `v3`) — otherwise the cached extension blob from the previous key is reused and the new extension never gets installed on the runner. `grep -n "key: php-ext" .github/workflows/ci.yml` enumerates both rows in one pass.
- **[gotcha]** PSR-4 autoload requires one class per file with basename = class name. Putting test helper/fixture classes (e.g. `UserJsonableMsg`, `TickModel`) at the bottom of a `FooTest.php` works ONLY because PHPUnit `require`s `FooTest.php` to instantiate the test, side-effect-loading the inline classes. If a SECOND test file references those fixtures and runs first in isolation (e.g. `phpunit --filter BarTest`), autoload fails. Defining a `final class TickModel` at the bottom of `tests/Assert/ScreenRoundTripTest.php` collides at autoload time if `tests/PlayerTest.php` also defines its own `TickModel` — both files declare the same FQCN even though each is local to one test, causing `Cannot declare class … because the name is already in use` when both load in the same suite. Either use a different per-file class name or extract the fixture to a single shared file.
- **[convention]** Plans under `plans/*.md` may carry an explicit conditional gate (e.g. `plans/x-xpty.md`: "only worth doing if we're going to upgrade candy-wish to host SSH sessions in-process … Document the upgrade path; don't ship until the use case is green-lit"). Before starting a slice, scan for phrases like "Conditional plan", "don't ship until", "only worth doing if", "green-lit" and confirm the gate is satisfied. If the gate isn't satisfied, surface that to the user before scaffolding instead of charging ahead.
- **[pattern]** For FFI-dependent test classes (e.g. `candy-pty`'s `Pty::open()` round-trip), do NOT put the FFI/`/dev/ptmx`/PHP_OS_FAMILY skip checks in `setUp()` — that skips the entire class on FFI-less CI runners and you lose coverage on structural tests (cdef shape, env-override resolution, exception hierarchy, ioctl-constant numbers) that don't actually touch the syscalls. Instead extract a private `requirePtySyscalls()` helper and call it as the FIRST line of each test that opens a real pty. Structural tests run unconditionally; round-trip tests skip cleanly when the host can't support them.
- **[gotcha]** `.github/workflows/vhs.yml` does NOT use a YAML `matrix:` list for libs — it uses a Bash array `all=( candy-core candy-sprinkles … )` inside an `id: compute` step (around lines 51-69). AGENTS.md says "matrix lib: entry" but that's only accurate for `ci.yml`. To register a new VHS-eligible lib, edit the bash array tokens (whitespace-separated, multi-line) — `grep` for an existing entry to locate the right slot.
- **[gotcha]** `proc_close($proc)` returns `-1` (NOT the real exit code) when the child has already been reaped via a prior `proc_get_status($proc)` call — `proc_get_status` itself reaps the zombie once `running===false`, leaving `proc_close` with nothing to wait on. Pattern: in any `wait()`-style helper, poll `proc_get_status()` until `running===false`, capture `['exitcode']` FIRST, then call `proc_close()` purely to release the resource handle and discard its return value. Re-querying `proc_get_status()` after the first `running===false` returns `exitcode === -1` for the same reason — cache the value on first observation.
- **[gotcha]** PHP FFI cannot declare variadic C signatures (`int ioctl(int fd, unsigned long request, ...)`) — the cdef parser rejects `...` and `void*` variadic forms. Workaround: declare each variadic libc call with the exact concrete third-arg shape you need, e.g. `int ioctl(int fd, unsigned long request, void *arg);` in `Libc::cdef()`, and only ever pass an `FFI::new('unsigned short[N]')` (or other CData) as that arg. Multiple shapes need either separate cdef declarations under different names (not possible with libc) or a `void*` arg + manual buffer construction per call site.
- **[convention]** `TIOCSWINSZ` / `TIOCGWINSZ` ioctl numbers DIVERGE between Linux and macOS — Linux uses compact type-2 encoding (`TIOCSWINSZ=0x5414`, `TIOCGWINSZ=0x5413` from `/usr/include/asm-generic/ioctls.h`), macOS uses BSD `_IOW`/`_IOR` macro encoding that packs direction/type/group/number into the top bits (`TIOCSWINSZ=0x80087467`, `TIOCGWINSZ=0x40087468` from `/usr/include/sys/ttycom.h`). Pick at runtime via `PHP_OS_FAMILY === 'Darwin' ? DARWIN_… : LINUX_…`. The `winsize` struct itself is identical on both — four little-endian unsigned shorts `[ws_row, ws_col, ws_xpixel, ws_ypixel]`.
- **[pattern]** To verify a PTY's slave-side winsize without needing a master-fd read primitive, spawn `tput cols` / `tput lines` in the child and redirect their STDOUT to a regular file: `['/bin/sh', '-c', 'tput cols > /tmp/foo; tput lines >> /tmp/foo']`. `tput` queries `TIOCGWINSZ` on STDIN (the slave PTY), not stdout — so redirecting stdout away doesn't affect the query, and you observe what the child actually saw via plain `file_get_contents`. Set `'TERM' => 'xterm-256color'` and a sane `PATH` in the env array; without `TERM`, tput exits non-zero with an unhelpful error.
