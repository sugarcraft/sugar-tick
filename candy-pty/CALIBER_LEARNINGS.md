# Caliber Learnings — candy-pty

Accumulated patterns and anti-patterns from development sessions.
Auto-managed by [caliber](https://github.com/caliber-ai-org/ai-setup) — do not edit manually.

- **[gotcha:tiocsctty]** PR2 wires the slave path to `proc_open()`'s `[0,1,2]` descriptor slots — opening `/dev/pts/N` three times — so the kernel hands the child stdio that talks to the master. But the child does NOT claim the slave as its **controlling terminal** (no `setsid()` + `ioctl(slave_fd, TIOCSCTTY, 0)`). Consequence: `Ctrl+C` typed into the master does NOT generate a `SIGINT` for the child — the kernel has nowhere to deliver job-control signals. Tests using `/bin/true`, `/bin/false`, `sleep`, `sh -c 'exit N'` are unaffected (no signal-aware logic). Anything interactive (vim, less, bash with line-editing) will mis-handle Ctrl+C. The fix is upstream creack/pty's pattern: ship a tiny shim binary or pcntl_fork shim that does `setsid(); ioctl(0, TIOCSCTTY, 0); execvp(...)` between proc_open and the actual cmd. Tracked for PR5 (signal forwarder) — until then, document the limitation in the lib README and avoid spawning interactive processes that rely on TTY-driven signals.
- **[gotcha:proc_close]** `proc_close()` returns `-1` (not the real exit code) when the child has already been reaped via a prior `proc_get_status()` call — `proc_get_status()` consumes the kernel `waitpid` slot the first time it sees `running=false`. Pattern that survives this: poll `proc_get_status()` until `running=false`, capture `$status['exitcode']` THEN call `proc_close()` purely to clean up the resource. Do not rely on `proc_close()`'s return value as the exit code. Canonical implementation in `candy-pty/src/Child.php::wait()`.
- **[pattern:php-fd-stream]** Wrapping a libc-allocated file descriptor (e.g. the master fd from `posix_openpt()`) in a PHP stream resource so it composes with `fread`/`fwrite`/`stream_select` is a one-liner: `fopen('php://fd/' . $fd, 'r+b')`. Works for arbitrary fds, not just 0/1/2. The wrapper TAKES OWNERSHIP of the fd — `fclose($stream)` closes the underlying fd, so `Pty::close()` must branch on whether the stream was ever materialised vs the raw `Libc::lib()->close($fd)` path. PR4 ships this pattern with a stream-vs-libc close branch in `Pty::close()`.
- **[gotcha:eintr-stream-select]** `stream_select()` returns `false` when interrupted by a signal (EINTR), e.g. when SIGWINCH arrives mid-`read()`. Throwing an exception on the false return aborts a perfectly legitimate timed read. The fix: wrap `stream_select` in a deadline-based retry loop — on `false`, call `pcntl_signal_dispatch()` to drain pending handlers, recompute the remaining timeout from the absolute deadline, and retry. Bail out only if the timeout elapses (return `null`) or pcntl is missing (can't distinguish EINTR from a real error → throw). Canonical implementation in `candy-pty/src/Pty.php::read()`.
- **[pattern:signal-handlers-must-not-throw]** PHP signal handlers installed via `pcntl_signal()` run in the middle of arbitrary user code — throwing an exception out of a handler corrupts the call stack and produces obscure crashes. Every callback the SignalForwarder accepts (size provider, reaper) is wrapped in `try { } catch (\Throwable) { /* swallow */ }`. The `isClosed()` short-circuit at the top of the SIGWINCH handler covers the common race where SIGWINCH arrives between `Pty::close()` and PHP shutdown — calling `resize()` on a closed Pty would throw, but the early-return makes the handler a clean no-op.
- **[deferred:tioscctty-shim]** PR5 ships SIGWINCH forwarding + EINTR retry + zombie reap, but does NOT yet claim the slave PTY as the child's controlling terminal — `proc_open` can't do that on its own and the workaround needs either (a) a tiny PHP shim invoked via proc_open that calls `setsid()` + `ioctl(0, TIOCSCTTY, 0)` + `pcntl_exec(...)` between proc_open and the actual cmd, or (b) a fork+exec via FFI declarations of `fork`, `setsid`, `ioctl`, `dup2`, `execvp`. Until this lands, Ctrl+C typed at the master will NOT deliver SIGINT to the spawned child — interactive shells (bash, vim, less) mis-handle line-editing and signal-driven UI. Track for the candy-wish in-process SSH upgrade where the shim becomes mandatory.
